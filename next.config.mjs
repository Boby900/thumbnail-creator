/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns:[
            {
                hostname: "affable-avocet-293.convex.cloud"
            }
        ]
    }
};

export default nextConfig;
